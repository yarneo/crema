/**
 * Entry point. The shell is deliberately empty for now: the port is landing
 * brain-first (recipe diff, convergence trail, attempt log), because that is
 * the part that is actually Crema and the part that can be tested without a
 * machine. The gateway client and views come next.
 */
export {};
