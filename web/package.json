{
  "name": "crema-skin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Crema — an AI dial-in coach as a Decent.app skin",
  "scripts": {
    "dev": "vite",
    "dev:lan": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "node --test --experimental-strip-types \"src/test/*.test.ts\"",
    "validate:manifest": "node scripts/validate-manifest.mjs",
    "release:zip": "npm run build && npm run validate:manifest && rm -f crema-skin.zip && (cd dist && zip -qr ../crema-skin.zip . -x '*.DS_Store')"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
